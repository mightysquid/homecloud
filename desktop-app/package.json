{
  "name": "homecloud",
  "version": "1.0.0",
  "description": "gui for easy personal home cloud set up using a vps",
  "main": "http://localhost:8000",
  "node-remote": "http://localhost:8000",
  "scripts": {
    "preinstall": "cd ng-app && npm i",
    "dev:start": "nw .",
    "dev:serve": "cd desktop-app && ng serve --port=8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mightysquid",
  "license": "ISC",
  "dependencies": {
    "nw": "^0.69.1"
  }
}
